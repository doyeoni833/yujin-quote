# 견적서 작성 페이지

브라우저에서 견적서를 작성하고 이미지(PNG)·엑셀로 저장하거나 휴대폰 공유창으로 바로 보내는 페이지입니다.
서버 없이 파일만으로 동작합니다. 사업자 정보, 입력 내용, 최근 견적은 각 기기의 브라우저에만 저장되며 어디로도 전송되지 않습니다.

- `index.html` : 페이지 전체 (입력 폼, 캔버스 렌더링, 엑셀 내보내기)
- `template.xlsx` : 엑셀 저장 시 값을 채워 넣는 빈 양식
- `manifest.json`, `icon.svg` : 휴대폰 홈 화면 추가용
